@using Microsoft.AspNetCore.Identity
@using LaPizzaria.Models
@inject SignInManager<ApplicationUser> SignInManager

@if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
{
    <aside class="admin-sidebar" id="adminSidebar">
        <div class="sidebar-header">
            <div class="sidebar-logo-container">
                <div class="sidebar-logo-icon">🍕</div>
                <div class="sidebar-logo-text-group">
                    <h6 class="sidebar-title">Pizza Admin</h6>
                    <span class="sidebar-subtitle">Quản trị viên</span>
                </div>
            </div>
            <button id="sidebarToggle" class="sidebar-toggle-btn">
                <i class="bi bi-chevron-left"></i>
            </button>
        </div>

        <nav class="sidebar-menu">
            <a asp-controller="Statistics" asp-action="Daily" class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Statistics" ? "active" : "")">
                <i class="bi bi-grid"></i> <span class="link-text">Thống Kê</span>
            </a>
            <a asp-controller="Product" asp-action="Index" class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Product" ? "active" : "")">
                <i class="bi bi-box-seam"></i> <span class="link-text">Quản Lý Sản Phẩm</span>
            </a>
            <a asp-controller="Table" asp-action="Index" class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Table" ? "active" : "")">
                <i class="bi bi-calendar-event"></i> <span class="link-text">Đặt Bàn</span>
            </a>
            <a asp-controller="Employee" asp-action="Index" class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Employee" ? "active" : "")">
                <i class="bi bi-people"></i> <span class="link-text">Nhân viên</span>
            </a>
            <a asp-controller="Ingredient" asp-action="Index" class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Ingredient" ? "active" : "")">
                <i class="bi bi-egg-fried"></i> <span class="link-text">Nguyên liệu</span>
            </a>
            <a asp-controller="Voucher" asp-action="Index" class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Voucher" ? "active" : "")">
                <i class="bi bi-ticket-perforated"></i> <span class="link-text">Voucher</span>
            </a>
            <a asp-controller="Customer" asp-action="Index" class="sidebar-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Customer" ? "active" : "")">
                <i class="bi bi-chat-dots"></i> <span class="link-text">Khách hàng</span>
            </a>
        </nav>

        <div class="sidebar-footer">
            <form asp-controller="Account" asp-action="Logout" method="post">
                <button type="submit" class="logout-btn">
                    <i class="bi bi-box-arrow-right"></i> <span class="link-text">Đăng xuất</span>
                </button>
            </form>
        </div>
    </aside>
}
